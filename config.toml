## The URL the site will be built for
base_url = "https://blog.servoj.ch/"

## Whether to automatically compile all Sass files in the sass directory
compile_sass = true
## Whether to build a search index to be used later on by a JavaScript library
build_search_index = true
## When set to "true", the generated HTML files are minified.
#minify_html = true
## When set to "true", a feed is automatically generated.
#generate_feeds = true
## When set to "all", paginated pages are not a part of the sitemap, default is "none"
#exclude_paginated_pages_in_sitemap = "none"
## The filenames to use for the feeds. Used as the template filenames, too.
## Defaults to ["atom.xml"], which has a built-in template that renders an Atom 1.0 feed.
## There is also a built-in template "rss.xml" that renders an RSS 2.0 feed.
feed_filenames = ["atom.xml", "rss.xml"]
## When set to "false", Sitemap.xml is not generated
generate_sitemap = true
# When set to "false", robots.txt is not generated
generate_robots_txt = true

default_language = "fr"
title = "Blog de LoskrAES"
description = "Bienvenue sur le blog de LoskrAES"
author = "LoskrAES"


[markdown]
## Whether to do syntax highlighting
## Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
error_on_missing_highlight = true
# Unicode emoji equivalent in the rendered Markdown files. (e.g.: :smile: => 😄)
render_emoji = true

## If this is true, a `rel="noopener"` will always automatically be added for security reasons
#external_links_target_blank = false
## Whether to set rel="nofollow" for all external links
#external_links_no_follow = false
## Whether to set rel="noreferrer" for all external links
#external_links_no_referrer = false

## For example, `...` into `…`, `"quote"` into `“curly”` etc
#smart_punctuation = false

## Whether to set decoding="async" and loading="lazy" for all images
## When turned on, the alt text must be plain text.
## For example, `![xx](...)` is ok but `![*x*x](...)` isn’t ok
#lazy_async_image = false

bottom_footnotes = true

[link_checker]
internal_level = "error"
external_level = "error"

[search]
include_title = true
include_description = false
include_date = false
include_path = false
include_content = true
## At which code point to truncate the content to. Useful if you have a lot of pages and the index would
## become too big to load on the site. Defaults to not being set.
#truncate_content_length = 100

[translations]
license_reserved = "tout droit reservé"
license_cc_by = "CC BY"

[languages.en]
title = "LoskrAES's Blog"
description = "Welcom on LoskrAES's Blog"
[languages.en.translations]
license_reserved = "all right reserved"
license_cc_by = "CC BY"


[extra]
## Put all your custom variables here

copyright_years = "2025"
default_license = "reserved"

mastodon_server = "tooting.ch"
mastodon_user = "loskraes"
